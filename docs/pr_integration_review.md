# PR 集成审查

日期：2026-09-12。用户授权审查并合并适合的内容到 main，通过 codex/integrate-reviewed-prs 的集成 PR 执行。

## 采纳与修复

- #7：问题3自建环境、基础策略、两阶段策略及完整依赖；保留 experiments 定位。
- #4：实验产物规范；保留已有实验名，将新运行产物统一写入 outputs/experiments，补齐实际索引。
- #5：B题基础文档；三份官方材料链接改为现存 problem 路径，重新核对 SHA-256，更新交接状态。移除把历史授权约定错误归因于当前 AGENTS.md 的表述。
- 两个运行入口严格检查输出位于各自 outputs/experiments/<实验目录>/<run-id> 内；运行记录追加命令参数。默认忽略可重建产物，保留既有固定案例和历史验证记录。

## 新鲜验证

18项单元检查通过；31秒示例确定性重放通过；基础策略4局、两套策略对12个保存案例的24局重放全部清除，逐动作计费和真值区域审计通过。两个入口的旧目录及路径越界共4项拒绝检查通过。命令、退出码与源码哈希见 [机器可读记录](pr_integration_validation.json)。

结果位于本地 outputs/experiments/b_adaptive_q3/merge_verified 与 outputs/experiments/b_benchmark_scale/merge_verified；可使用记录内命令和新的 run-id 复现。官方三份原件哈希与题意契约相符，未修改。检查新增文档链接、UTF-8、冲突标记及 git diff --check。

## 暂不合并

| PR | 原因 |
|---|---|
| #6 | 2732个文件含大量历史产物，与#7代码重叠且版本有差异；未全面验证，按需求另行选择模块 |
| #3 | 空模板状态已过时，同名概况和交接由#5填充文档提供 |
| #1 | 训练资料采用成员名顶层 hqy，违反目录边界约定；不属于当前模拟器交付依赖 |

没有调用官方接口或消耗正式测试次数；本地成绩不等于官方成绩。没有将实验代码晋升到 src，团队对核心成果的人工核验仍需完成。
