# 重大决策记录

本文件记录会长期影响模型结果、论文表达或团队协作方式的决定。日常任务、临时进度和成员分工不写在这里。

新增决策时依次写明：日期、状态、背景、决定、理由、备选方案和影响。状态使用“提议”“已接受”“已替代”或“已撤销”；替代既有决策时保留原记录，并链接到新决定。

## D-001：项目目录采用文件生命周期分区

- 日期：2026-09-05
- 状态：已接受
- 背景：项目需要支持多人在短周期内并行探索模型、生成结果和撰写论文，同时保持官方输入不被污染、论文结果可追溯。
- 决定：采用 `inputs → experiments → src → outputs → paper → submission` 的单向工作流；仅当出现新的生命周期边界时才增加顶级目录。
- 理由：不同阶段的文件在来源、可修改性、生成方式和交付方式上具有不同约束。按生命周期分区能够减少试验代码污染、结果版本混淆和最终提交错误。
- 备选方案：通用数据科学多层目录、按工具划分目录、按团队成员划分目录。前者在当前阶段过度设计，后两者会割裂同一成果链路。
- 影响：官方输入只读；试验代码晋升后才进入正式计算链；生成结果与论文源稿分离；提交物从冻结版本产生。

## 新决策记录格式

复制以下字段并使用下一个连续编号：

```text
## D-编号：决策标题

- 日期：YYYY-MM-DD
- 状态：提议｜已接受｜已替代｜已撤销
- 背景：需要解决的问题和约束
- 决定：团队选择的明确方案
- 理由：支持该选择的证据和权衡
- 备选方案：考虑过但未采用的方案及原因
- 影响：涉及的数据、代码、论文、提交物和协作方式
```
