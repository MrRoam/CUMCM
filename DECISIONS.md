# 重大决策记录

本文件记录会长期影响模型结果、论文表达或团队协作方式的决定。日常任务、临时进度和成员分工不写在这里。

新增决策时依次写明：日期、状态、背景、决定、理由、备选方案和影响。状态使用“提议”“已接受”“已替代”或“已撤销”；替代既有决策时保留原记录，并链接到新决定。

## D-001：项目目录采用文件生命周期分区

- 日期：2026-09-05
- 状态：已接受
- 背景：项目需要支持多人在短周期内并行探索模型、生成结果和撰写论文，同时保持官方输入不被污染、论文结果可追溯。
- 决定：采用 `inputs → experiments → src → outputs → paper → submission` 的单向工作流；仅当出现新的生命周期边界时才增加顶级目录。
- 理由：不同阶段的文件在来源、可修改性、生成方式和交付方式上具有不同约束。按生命周期分区能够减少试验代码污染、结果版本混淆和最终提交错误。
- 备选方案：通用数据科学多层目录、按工具划分目录、按团队成员划分目录。前者在当前阶段过度设计，后两者会割裂同一成果链路。
- 影响：官方输入只读；试验代码晋升后才进入正式计算链；生成结果与论文源稿分离；提交物从冻结版本产生。

## D-002：C 题建模前的关键口径提议

- 日期：2026-09-10
- 状态：已撤销
- 背景：C 题题面与模板存在 10 分钟时间标签冲突；未来信息可得性、调减购电退款、多次调整结算及问题 2—4 的日末储能要求未唯一说明。
- 决定：尚未正式采纳。审题提出：内部使用自然日 144 段并暂按数据右端标签解释；事前策略只使用当时可得信息；充放电效率暂各取 90%、能量和功率按母线侧定义；问题 2—4 连续衔接储电量；下调购电暂按保留原计划款并另收违约费理解，同时保留其他结算解释。多次调整结算和模板导出仍需明确。
- 理由：上述解释便于保持时间边界、信息顺序、物理账与费用账一致，但现有材料不足以将其全部确认为官方要求。
- 备选方案：模板左端时间解释、提前已知实况情景、往返效率 90%、逐日储能循环、调减退款后收罚金及其他结算方式。历史自动核验证据保存在 `experiments/archive/C题/c_framing_evidence.json`，原审题文档未随当前主线保留。
- 影响：团队已经选择 B 题，本提议不再进入正式建模、论文或结果文件；保留本记录仅用于说明历史过程。

## D-003：正式选择 B 题作为当前竞赛题目

- 日期：2026-09-10
- 状态：已接受
- 背景：团队已经完成 B、C 两题的初步审题，需要确定唯一主线，避免模型、输入、文档和论文工作继续分散。
- 决定：正式选择 B 题“无线电干扰源的快速自动定位与清除”；停止将 C 题作为当前建模方向。B 题官方原件归入 `inputs/official/B题/`，可检索转换稿和分析材料归入 `docs/B题/`，C 题已有核验脚本和证据只作历史归档。
- 理由：团队已明确作出选题决定；统一主线有利于集中完成定位几何、全域发现、路径调度、模拟器通信和验证工作。
- 备选方案：选择 C 题，或继续并行推进 B、C 两题。前者不符合团队当前决定，后者会消耗有限竞赛时间并造成唯一事实源混乱。
- 影响：README、目录索引、后续实验、正式代码、结果和论文均以 B 题为准；C 题历史材料不得误用为 B 题依据。

## D-004：第一问采用方法 A 与旋转卡壳，单独建立可复核实现

- 日期：2026-09-11
- 状态：已接受（用户已明确选择方法 A；实现结果的人工复核待完成）
- 背景：第一问需从测向约束给出定位多边形直径，并判断直径圆覆盖；全配对求交不必要，原参考实现有复杂度及边界判定问题。
- 决定：第一问采用排序＋双端队列半平面交、旋转卡壳及直径端点中点圆的顶点覆盖判据，直接实现到 `src/q1/`。本模块选用本机已可用的 Python 3.13 标准库，以整数/有理数谓词保证转换后几何输入的精确判定；用有整数系数界证明的辅助框区分有界与无界。输出只写 `outputs/q1/`。
- 理由：用户已经指定核心算法，不再以候选试验处理；该实现便于说明复杂度、独立对照和验证退化情况。正式源码与人工构造测试集中维护，不引入第三方依赖或新增顶级目录。
- 备选方案：逐步多边形裁剪仍适合后续在线更新，但不是本次选定的批量方法；浮点 EPS 实现速度可能较好，但近乎平行、退化和大框误判需额外处理；对偶凸包不在本次实现范围。
- 影响：第一问使用纯测向楔域的多边形口径，不混入圆域、距离排除或模拟器状态；O(n log n) 指算术操作次数，任意精度位成本另计；三角函数仍存在初始浮点舍入。此决定不替团队确定其他小问工具链，也不代表人工核验、论文采纳或模拟器验证已经完成。

## D-005：第二问两种选点策略的试验比较口径

- 日期：2026-09-11
- 状态：已替代（探索比较保留；当前方案见D-006）
- 背景：需比较中垂线最大交会角与全平面平均半径搜索，避免把不同接收约束、半径含义或取点权重混在一起。
- 决定：本轮半径统一取交集最小包围圆半径；第一种方案最大化所有可能源位置的最小锐交会角；两者均在max距离≤1000米的保守接收集合内比较。平均值分别使用面积均匀与径向均匀确定性网格，二次误差包含均匀网格及零误差对照；这些分布仅作为探索假设。
- 理由：用户明确确认上述交会角和半径含义；等权离散点的分布会改变目标函数，需做口径敏感性核验。统一接收条件避免通过删除失联案例获得虚假优势。
- 备选方案：直径的一半、连续最坏半径、联合位置与接收半径的更大可行集、完整定位清除耗时；本轮不作为主目标。
- 影响：在原点完整扇形的标准算例中，平面搜索平均半径改善约0.5%—5%，不足以证明后续任务更快或所有位置上均更优。脚本留在experiments，结论须经团队审查后再决定是否晋升与扩展。

## D-006：第二问暂选面积均匀平均最小包围圆半径策略

- 日期：2026-09-11
- 状态：已接受（用户已明确选择；团队人工核验与一般场景扩展待完成）
- 背景：面积均匀平均半径最优点与离散最坏半径最优点相距约51.537米，但从第一次检测点出发的路程仅差0.138米，按5米/秒折算仅差0.028秒；最坏半径和面积平均半径的相对交换均约0.34%，对当前策略判断影响很小。
- 决定：第二问在当前标准完整扇形场景中，暂采用“面积均匀下最小化两次测向交集的平均最小包围圆半径”作为选点目标。标准局部坐标中的第二点取(870.000, ±501.683285691)米；正负号表示首次示向中心线两侧的镜像等价方案。正式实现、配置、测试和AI接续说明归入`src/q2/`，结果写入`outputs/q2/`。
- 理由：面积均匀比径向均匀更符合对二维扇形区域无额外先验时的均匀位置假设；平均值点与离散最坏值点的时间差和定位性能差异均很小，因此暂不为极小的离散最坏值改善改变主目标。平均指标也更容易解释标准场景下的总体定位质量。
- 备选方案：离散最坏值点(843.000,545.581798557)米可在更强调风险上界时启用，但当前有限网格最坏值不是连续严格上界；融合指标增加了未由题面给定的权重，暂不采用；中垂线最大最小交会角保留为简单基线。
- 影响：后续第二问接续以`src/q2/strategy_config.json`为口径事实源，不再重复选择面积均匀与径向均匀。当前坐标只能对未被1800米目标圆域等额外边界裁剪的标准首次扇形做刚体变换；一般初始位置仍需重建区域并重新优化。不得将“当前搜索最佳”写成连续全局最优、官方概率模型或完整任务耗时最优。

## 新决策记录格式

复制以下字段并使用下一个连续编号：

```text
## D-编号：决策标题

- 日期：YYYY-MM-DD
- 状态：提议｜已接受｜已替代｜已撤销
- 背景：需要解决的问题和约束
- 决定：团队选择的明确方案
- 理由：支持该选择的证据和权衡
- 备选方案：考虑过但未采用的方案及原因
- 影响：涉及的数据、代码、论文、提交物和协作方式
```
